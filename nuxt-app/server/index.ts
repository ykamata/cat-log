// import express, { Request, Response } from 'express';

// const app = express();
// const port = process.env.PORT || 3001;

// app.get('/', (req: Request, res: Response) => {
//   res.send('Hello from the backend!');
// });

// app.listen(port, () => {
//   console.log(`Backend server listening at http://localhost:${port}`);
// });

// Nuxt 3 uses the /server/api directory for API routes by default.
// This file can be used for custom server middleware if needed,
// but for simple API routes like the ones generated, it's not strictly necessary
// to have a custom Express app running here.
// If specific middleware or advanced server configurations are needed,
// this file would be the place, using Nitro's event handler system.
// For now, ensure API calls go through Nuxt's default handling.
